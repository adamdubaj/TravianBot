package travianAPI.Items;

import java.nio.file.Path;

public class Mount extends Item {
    public Mount(String id, String name, String description, Path picture, int amount) {
        super(id, name, description, picture, amount);
    }
}
